name: deploy-omslagroute

trigger: none

variables:
  System.Debug: true

resources:
  repositories:
    - repository: azure-templates
      type: git
      name: salmagundi/azure-templates
      ref: refs/heads/tamm-late

extends:
  template: pipelines/aks/template.yaml@azure-templates
  parameters:
    name: deploy-omslagroute
    build:
      pool:
        name: ContainerBuilder
    dev:
      pool: salmagundi
      namespace: ns-ccc-mrkt-ont-01-wonen
      kubernetesServiceConnection: SCN-K8S-ns-ccc-mrkt-ont-01-wonen